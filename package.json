{
  "name": "production-line-diary",
  "version": "0.1.0",
  "description": "產線工作日誌 — 設備異常紀錄與可視化儀表板",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env backend/server.js",
    "dev": "node --env-file-if-exists=.env --watch backend/server.js",
    "migrate": "node --env-file-if-exists=.env scripts/migrate.js",
    "test": "node --test",
    "check": "node --check backend/server.js && node --check backend/lib/aggregate.js && node --check backend/lib/store.js && node --check backend/lib/xlsx.js && node --check backend/lib/migrateLogic.js && node --check backend/lib/photos.js && node --check scripts/migrate.js"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "express": "^4.19.2",
    "multer": "^2.4.0"
  }
}
